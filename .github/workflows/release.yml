name: Release

on:
  push:
    branches: [release/stable]

permissions:
  contents: write
  actions: read

concurrency:
  group: plexoncrates-stable
  cancel-in-progress: false

env:
  GUI_BASE_TAG: 'v6.0.0-rc.1'
  GUI_BASE_SHA: 'cb3ba1aed8ab2b89bdbaec39271ad30ee07e1c28'
  GUI_SOURCE_EQUIVALENT_HEAD: 'c865e64e00fb7f33975dd362d9f43766156898c9'
  ROLLBACK_TAG: 'v6.0.0-rc.1'
  ROLLBACK_SHA: 'cb3ba1aed8ab2b89bdbaec39271ad30ee07e1c28'
  PLEXONCORE_SHA256: '61d625a717da9f46ee9231e1970d84b4c317ae12cf4090cdf7c9d39b6a1a9baf'
  PLEXONKEYS_VERSION: '2.0.0-rc.2'
  PLEXONKEYS_SHA256: '0cc87449754b7e5dec34e57091f856f2bfa98a3c256eb41617b510911cbe0ee4'

jobs:
  release:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout exact stable candidate
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Java 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'
          cache: maven

      - name: Provision immutable Plexon API dependencies
        shell: bash
        run: |
          set -euo pipefail
          core="${RUNNER_TEMP}/PlexonCore-2.0.4.jar"
          keys="${RUNNER_TEMP}/PlexonKeys-${PLEXONKEYS_VERSION}.jar"
          curl --fail --location --silent --show-error --retry 3 -o "$core" \
            https://github.com/ZpkDxGames/PlexonCore/releases/download/v2.0.4/PlexonCore-2.0.4.jar
          echo "${PLEXONCORE_SHA256}  $core" | sha256sum --check
          curl --fail --location --silent --show-error --retry 3 -o "$keys" \
            "https://github.com/ZpkDxGames/PlexonKeys/releases/download/v${PLEXONKEYS_VERSION}/PlexonKeys-${PLEXONKEYS_VERSION}.jar"
          echo "${PLEXONKEYS_SHA256}  $keys" | sha256sum --check
          mvn -B -ntp org.apache.maven.plugins:maven-install-plugin:3.1.4:install-file \
            -Dfile="$core" -DgroupId=com.zpkdxgames -DartifactId=PlexonCore -Dversion=2.0.4 -Dpackaging=jar -DgeneratePom=true
          mvn -B -ntp org.apache.maven.plugins:maven-install-plugin:3.1.4:install-file \
            -Dfile="$keys" -DgroupId=com.antondev -DartifactId=PlexonKeys \
            -Dversion="$PLEXONKEYS_VERSION" -Dpackaging=jar -DgeneratePom=true

      - name: Resolve exact release version
        shell: bash
        run: |
          set -euo pipefail
          version="$(mvn -q -DforceStdout help:evaluate -Dexpression=project.version)"
          test "$version" = '6.5.0'
          [[ "$version" != *-* ]]
          echo "PLUGIN_VERSION=$version" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=v${version}" >> "$GITHUB_ENV"

      - name: Verify exact 6.5 GUI-only stable boundary
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          main_sha="$(gh api "repos/$GITHUB_REPOSITORY/git/ref/heads/main" --jq '.object.sha')"
          test "$GITHUB_SHA" = "$main_sha"
          test "$(mvn -q -DforceStdout help:evaluate -Dexpression=paper.version)" = '26.2.build.121-stable'
          test "$(mvn -q -DforceStdout help:evaluate -Dexpression=plexoncore.version)" = '2.0.4'
          test "$(mvn -q -DforceStdout help:evaluate -Dexpression=plexonkeys.version)" = "$PLEXONKEYS_VERSION"
          test "$(git rev-list -n 1 "$GUI_BASE_TAG")" = "$GUI_BASE_SHA"
          test "$(git rev-list -n 1 "$ROLLBACK_TAG")" = "$ROLLBACK_SHA"
          git merge-base --is-ancestor "$GUI_BASE_SHA" HEAD
          git merge-base --is-ancestor "$GUI_SOURCE_EQUIVALENT_HEAD" HEAD
          test -s "releases/${PLUGIN_VERSION}.md"

          while IFS= read -r changed; do
            case "$changed" in
              CHANGELOG.md|README.md|pom.xml|docs/*|"releases/${PLUGIN_VERSION}.md"|\
              .github/workflows/build.yml|.github/workflows/release.yml|\
              src/main/resources/menus.yml|\
              src/main/java/com/antondev/crates/config/MenuConfig.java|\
              src/main/java/com/antondev/crates/config/GuiConfigMigrator.java|\
              src/main/java/com/antondev/crates/gui/*|\
              src/test/java/*)
                ;;
              *)
                echo "6.5 GUI-only stable scope violation: $changed" >&2
                exit 1
                ;;
            esac
          done < <(git diff --name-only "$GUI_BASE_SHA"..HEAD)

          for protected in \
            src/main/java/com/antondev/crates/service/OpeningService.java \
            src/main/java/com/antondev/crates/service/ClaimService.java \
            src/main/java/com/antondev/crates/database/DatabaseService.java \
            src/main/java/com/antondev/crates/item/ItemSnapshotCodec.java \
            src/main/java/com/antondev/crates/service/RewardSelector.java; do
            if ! git diff --quiet "$GUI_BASE_SHA"..HEAD -- "$protected"; then
              echo "6.5 GUI-only stable authority changed: $protected" >&2
              exit 1
            fi
          done
          git diff --check "$GUI_BASE_SHA"..HEAD

          if gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$RELEASE_TAG" >/dev/null 2>&1; then
            echo "$RELEASE_TAG already exists; refusing to move a stable tag." >&2
            exit 1
          fi
          if gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "$RELEASE_TAG release already exists; refusing replacement." >&2
            exit 1
          fi

      - name: Test and package exact stable source
        run: mvn -B -ntp clean verify

      - name: Verify exact distribution
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import glob, pathlib, xml.etree.ElementTree as ET
          t=f=e=s=0
          reports=glob.glob('target/surefire-reports/TEST-*.xml')
          if not reports:
              raise SystemExit('No JUnit XML results found')
          for path in reports:
              for case in ET.parse(path).getroot().iter('testcase'):
                  t += 1
                  f += case.find('failure') is not None
                  e += case.find('error') is not None
                  s += case.find('skipped') is not None
          if not t or f or e or s:
              raise SystemExit(f'tests={t} failures={f} errors={e} skipped={s}')
          pathlib.Path('target/TEST_SUMMARY.txt').write_text(
              f'TEST_COUNT={t} FAILURES={f} ERRORS={e} SKIPPED={s}\n')
          PY
          JAR="target/PlexonCrates-${PLUGIN_VERSION}.jar"
          test -s "$JAR"
          jar tf "$JAR" > target/jar-contents.txt
          for required in \
            plugin.yml menus.yml \
            com/antondev/crates/PlexonCrates.class \
            com/antondev/crates/gui/GuiTheme.class \
            com/antondev/crates/gui/GuiLayout.class \
            com/antondev/crates/gui/GuiChromeRenderer.class \
            com/antondev/crates/config/GuiConfigMigrator.class \
            com/antondev/crates/service/OpeningService.class \
            com/antondev/crates/service/ClaimService.class \
            org/sqlite/JDBC.class; do
            grep -qx "$required" target/jar-contents.txt
          done
          unzip -p "$JAR" plugin.yml | grep -F "version: '${PLUGIN_VERSION}'"
          unzip -p "$JAR" plugin.yml | grep -F "api-version: '26.2'"
          unzip -p "$JAR" META-INF/MANIFEST.MF | tr -d '\r' | grep -F "Implementation-Version: ${PLUGIN_VERSION}"
          javap -verbose -classpath "$JAR" com.antondev.crates.PlexonCrates | grep -F 'major version: 69'
          for prefix in \
            'com/zpkdxgames/plexoncore/' 'com/antondev/keys/' 'net/milkbowl/vault/' \
            'me/clip/placeholderapi/' 'net/luckperms/api/' 'org/bukkit/' 'net/kyori/adventure/'; do
            if grep -q "^${prefix}" target/jar-contents.txt; then
              echo "Forbidden dependency shading: ${prefix}" >&2
              exit 1
            fi
          done
          (cd target && sha256sum "PlexonCrates-${PLUGIN_VERSION}.jar" > SHA256SUMS.txt && sha256sum --check SHA256SUMS.txt)

      - name: Build exact release evidence
        shell: bash
        run: |
          set -euo pipefail
          JAR="target/PlexonCrates-${PLUGIN_VERSION}.jar"
          size="$(stat -c '%s' "$JAR")"
          digest="$(sha256sum "$JAR" | awk '{print $1}')"
          tests="$(sed -n 's/^TEST_COUNT=\([0-9][0-9]*\).*/\1/p' target/TEST_SUMMARY.txt)"
          {
            echo "repository=$GITHUB_REPOSITORY"
            echo "source_sha=$GITHUB_SHA"
            echo "version=$PLUGIN_VERSION"
            echo 'release_kind=STABLE'
            echo "gui_base_tag=$GUI_BASE_TAG"
            echo "gui_base_sha=$GUI_BASE_SHA"
            echo "rollback_tag=$ROLLBACK_TAG"
            echo "rollback_sha=$ROLLBACK_SHA"
            echo 'java=25'
            echo 'java_major=69'
            echo 'paper=26.2.build.121-stable'
            echo 'plexoncore=2.0.4'
            echo "plexoncore_sha256=$PLEXONCORE_SHA256"
            echo "plexonkeys=$PLEXONKEYS_VERSION"
            echo "plexonkeys_sha256=$PLEXONKEYS_SHA256"
            echo 'database_schema=4'
            echo "jar_size=$size"
            echo "jar_sha256=$digest"
            echo "test_count=$tests"
            echo 'runtime_certification=NOT_EXECUTED_GUI_ONLY_RELEASE'
            echo 'verification_scope=SOURCE_SCOPE_PLUS_AUTOMATED_REGRESSION'
          } > target/PROVENANCE.txt

      - name: Publish GitHub stable release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh release create "$RELEASE_TAG" \
            "target/PlexonCrates-${PLUGIN_VERSION}.jar" \
            target/SHA256SUMS.txt target/TEST_SUMMARY.txt target/PROVENANCE.txt \
            --repo "$GITHUB_REPOSITORY" --target "$GITHUB_SHA" \
            --title "PlexonCrates ${PLUGIN_VERSION}" \
            --notes-file "releases/${PLUGIN_VERSION}.md" --latest

      - name: Verify published stable release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag_sha="$(gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$RELEASE_TAG" --jq '.object.sha')"
          test "$tag_sha" = "$GITHUB_SHA"
          release_json="$(gh release view "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --json tagName,isPrerelease,targetCommitish,assets)"
          echo "$release_json" | jq -e --arg tag "$RELEASE_TAG" --arg sha "$GITHUB_SHA" \
            '.tagName == $tag and .isPrerelease == false and .targetCommitish == $sha' >/dev/null
          actual_assets="$(echo "$release_json" | jq -r '.assets[].name' | sort)"
          expected_assets="$(printf '%s\n' "PlexonCrates-${PLUGIN_VERSION}.jar" PROVENANCE.txt SHA256SUMS.txt TEST_SUMMARY.txt | sort)"
          diff -u <(printf '%s\n' "$expected_assets") <(printf '%s\n' "$actual_assets")
          rm -rf published && mkdir published
          gh release download "$RELEASE_TAG" --repo "$GITHUB_REPOSITORY" --dir published \
            --pattern "PlexonCrates-${PLUGIN_VERSION}.jar" \
            --pattern 'SHA256SUMS.txt' --pattern 'TEST_SUMMARY.txt' --pattern 'PROVENANCE.txt'
          test "$(find published -maxdepth 1 -type f | wc -l)" -eq 4
          (cd published && sha256sum --check SHA256SUMS.txt)
          grep -Fx "repository=$GITHUB_REPOSITORY" published/PROVENANCE.txt
          grep -Fx "source_sha=$GITHUB_SHA" published/PROVENANCE.txt
          grep -Fx "version=$PLUGIN_VERSION" published/PROVENANCE.txt
          grep -Fx 'release_kind=STABLE' published/PROVENANCE.txt
          grep -Fx 'runtime_certification=NOT_EXECUTED_GUI_ONLY_RELEASE' published/PROVENANCE.txt
          grep -Eq '^TEST_COUNT=[1-9][0-9]* FAILURES=0 ERRORS=0 SKIPPED=0$' published/TEST_SUMMARY.txt

      - name: Upload release verification evidence
        uses: actions/upload-artifact@v4
        with:
          name: PlexonCrates-${{ github.sha }}-stable-release
          path: |
            target/PlexonCrates-*.jar
            target/SHA256SUMS.txt
            target/TEST_SUMMARY.txt
            target/PROVENANCE.txt
          if-no-files-found: error
