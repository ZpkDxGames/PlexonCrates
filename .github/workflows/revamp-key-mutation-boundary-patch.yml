name: Revamp key mutation IO boundary patch

on:
  push:
    branches:
      - revamp/5.1.0-full-source-hardening
    paths:
      - .github/workflows/revamp-key-mutation-boundary-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: revamp/5.1.0-full-source-hardening
          fetch-depth: 0
      - name: Apply staged key mutation boundary
        run: python tools/revamp_patch_key_mutation_boundary.py
      - name: Commit source patch and remove temporary helpers
        run: |
          git rm .github/workflows/revamp-key-mutation-boundary-patch.yml tools/revamp_patch_key_mutation_boundary.py
          git add src/main/java/com/antondev/crates/service/KeyService.java
          git config user.name "Tonim"
          git config user.email "99552287+ZpkDxGames@users.noreply.github.com"
          git commit -m "refactor: split key mutation mirror IO and activation"
          git push origin HEAD:revamp/5.1.0-full-source-hardening
