name: Revamp fully asynchronous live reload patch

on:
  push:
    branches:
      - revamp/5.1.0-full-source-hardening
    paths:
      - .github/workflows/revamp-reload-preload-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: revamp/5.1.0-full-source-hardening
          fetch-depth: 0
      - name: Apply live reload preload boundary
        run: python tools/revamp_patch_reload_preload.py
      - name: Commit source patch and remove temporary helpers
        run: |
          git rm .github/workflows/revamp-reload-preload-patch.yml tools/revamp_patch_reload_preload.py
          git add src/main/java/com/antondev/crates/PlexonCrates.java \
                  src/main/java/com/antondev/crates/gui/AdminMenuService.java \
                  src/main/java/com/antondev/crates/gui/MenuService.java
          git config user.name "Tonim"
          git config user.email "99552287+ZpkDxGames@users.noreply.github.com"
          git commit -m "perf: preload live reload files off the server thread"
          git push origin HEAD:revamp/5.1.0-full-source-hardening
