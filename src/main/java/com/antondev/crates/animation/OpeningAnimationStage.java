package com.antondev.crates.animation;

/** Ordered presentation stages shared by every opening animation profile. */
public enum OpeningAnimationStage {
    START,
    CHARGE,
    SELECTION,
    REVEAL,
    CELEBRATION,
    FINISH
}
